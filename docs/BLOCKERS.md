# Blockers

Only the founder can clear these.

- Sentry must allow `arena.vikisol.in` (Sentry project, Allowed Domains). Until then, ignore Sentry's own 403 in the tests.
- Before real money or a public push: a lawyer-reviewed privacy policy and terms, GST and Razorpay KYC, a pentest, and verified backups.
- The GitHub login used here cannot change GitHub Actions workflow files. It lacks the workflow scope.
